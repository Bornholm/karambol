<?php

namespace {{pluginNamespace}}\{{pluginName}};

use Karambol\KarambolApp;
use Karambol\Plugin\Plugin;
use {{pluginNamespace}}\{{pluginName}}\Controller;

class {{pluginName}}Plugin extends Plugin {

  public function boot(KarambolApp $app, array $options) {

    parent::boot($app, $options);

    // Add your views
    $this->registerViews(__DIR__.'/Views');

    // Add your entities
    // You will need to run "bin/migrate" to update the database schema
    $this->registerEntities(__DIR__.'/Entity');

    // Add your own translation files
    $this->registerTranslation('fr', __DIR__.'/../locales/fr.yml');

    // Add your own controllers
    $this->registerControllers([
      Controller\MyController::class
    ]);

  }

}
